import React from 'react';
import ReactDOM from 'react-dom/client';
import PerfectClockApp from './clock_eng';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PerfectClockApp />
  </React.StrictMode>
);
